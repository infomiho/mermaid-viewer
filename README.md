# Mermaid Viewer

![Mermaid Viewer](./public/social-card-v3.png)

Minimal static Mermaid previewer.

```bash
npm install
npm run dev
```

Build static assets with `npm run build`.

Deploy to the `mermaid` Buzz subdomain with `npm run deploy`.
